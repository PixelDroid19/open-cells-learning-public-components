import { OpenCellsPanelContainer } from './panel-container.js';

if (customElements.get('open-cells-panel-container') === undefined) customElements.define('open-cells-panel-container', OpenCellsPanelContainer);

export { OpenCellsPanelContainer };
