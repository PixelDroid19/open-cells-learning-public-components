import { OpenCellsListBullet } from './list-bullet.js';

if (customElements.get('open-cells-list-bullet') === undefined) customElements.define('open-cells-list-bullet', OpenCellsListBullet);

export { OpenCellsListBullet };
