import { OpenCellsFormSign } from './form-sign.js';

if (customElements.get('open-cells-form-sign') === undefined) customElements.define('open-cells-form-sign', OpenCellsFormSign);

export { OpenCellsFormSign };
