import { OpenCellsFormMultiselect } from './form-multiselect.js';

if (customElements.get('open-cells-form-multiselect') === undefined) customElements.define('open-cells-form-multiselect', OpenCellsFormMultiselect);

export { OpenCellsFormMultiselect };
