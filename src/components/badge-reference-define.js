import { OpenCellsBadgeReference } from './badge-reference.js';

if (customElements.get('open-cells-badge-reference') === undefined) customElements.define('open-cells-badge-reference', OpenCellsBadgeReference);

export { OpenCellsBadgeReference };
