import { OpenCellsBadgeIcon } from './badge-icon.js';

if (customElements.get('open-cells-badge-icon') === undefined) customElements.define('open-cells-badge-icon', OpenCellsBadgeIcon);

export { OpenCellsBadgeIcon };
