import { OpenCellsTableHeader } from './table-header.js';

if (customElements.get('open-cells-table-header') === undefined) customElements.define('open-cells-table-header', OpenCellsTableHeader);

export { OpenCellsTableHeader };
