import { OpenCellsDisableMixin } from './disable-mixin.js';

if (customElements.get('open-cells-disable-mixin') === undefined) customElements.define('open-cells-disable-mixin', OpenCellsDisableMixin);

export { OpenCellsDisableMixin };
