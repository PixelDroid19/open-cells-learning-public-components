import { OpenCellsPanelCodeNumber } from './panel-code-number.js';

if (customElements.get('open-cells-panel-code-number') === undefined) customElements.define('open-cells-panel-code-number', OpenCellsPanelCodeNumber);

export { OpenCellsPanelCodeNumber };
