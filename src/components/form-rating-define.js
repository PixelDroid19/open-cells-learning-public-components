import { OpenCellsFormRating } from './form-rating.js';

if (customElements.get('open-cells-form-rating') === undefined) customElements.define('open-cells-form-rating', OpenCellsFormRating);

export { OpenCellsFormRating };
