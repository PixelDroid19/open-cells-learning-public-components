import { OpenCellsClipCard } from './clip-card.js';

if (customElements.get('open-cells-clip-card') === undefined) customElements.define('open-cells-clip-card', OpenCellsClipCard);

export { OpenCellsClipCard };
