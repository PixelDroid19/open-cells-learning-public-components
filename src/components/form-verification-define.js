import { OpenCellsFormVerification } from './form-verification.js';

if (customElements.get('open-cells-form-verification') === undefined) customElements.define('open-cells-form-verification', OpenCellsFormVerification);

export { OpenCellsFormVerification };
