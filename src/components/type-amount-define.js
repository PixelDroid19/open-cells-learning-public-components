import { OpenCellsTypeAmount } from './type-amount.js';

if (customElements.get('open-cells-type-amount') === undefined) customElements.define('open-cells-type-amount', OpenCellsTypeAmount);

export { OpenCellsTypeAmount };
