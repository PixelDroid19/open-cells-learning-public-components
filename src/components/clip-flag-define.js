import { OpenCellsClipFlag } from './clip-flag.js';

if (customElements.get('open-cells-clip-flag') === undefined) customElements.define('open-cells-clip-flag', OpenCellsClipFlag);

export { OpenCellsClipFlag };
