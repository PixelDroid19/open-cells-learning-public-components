import { OpenCellsListSwipe } from './list-swipe.js';

if (customElements.get('open-cells-list-swipe') === undefined) customElements.define('open-cells-list-swipe', OpenCellsListSwipe);

export { OpenCellsListSwipe };
