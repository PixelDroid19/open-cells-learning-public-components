import { OpenCellsFormCalendar } from './form-calendar.js';

if (customElements.get('open-cells-form-calendar') === undefined) customElements.define('open-cells-form-calendar', OpenCellsFormCalendar);

export { OpenCellsFormCalendar };
