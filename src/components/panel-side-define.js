import { OpenCellsPanelSide } from './panel-side.js';

if (customElements.get('open-cells-panel-side') === undefined) customElements.define('open-cells-panel-side', OpenCellsPanelSide);

export { OpenCellsPanelSide };
