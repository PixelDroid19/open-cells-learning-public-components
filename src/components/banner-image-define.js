import { OpenCellsBannerImage } from './banner-image.js';

if (customElements.get('open-cells-banner-image') === undefined) customElements.define('open-cells-banner-image', OpenCellsBannerImage);

export { OpenCellsBannerImage };
