import { OpenCellsFormFilter } from './form-filter.js';

if (customElements.get('open-cells-form-filter') === undefined) customElements.define('open-cells-form-filter', OpenCellsFormFilter);

export { OpenCellsFormFilter };
