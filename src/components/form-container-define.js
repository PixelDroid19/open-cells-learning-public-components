import { OpenCellsFormContainer } from './form-container.js';

if (customElements.get('open-cells-form-container') === undefined) customElements.define('open-cells-form-container', OpenCellsFormContainer);

export { OpenCellsFormContainer };
