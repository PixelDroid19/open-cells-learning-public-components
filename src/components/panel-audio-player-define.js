import { OpenCellsPanelAudioPlayer } from './panel-audio-player.js';

if (customElements.get('open-cells-panel-audio-player') === undefined) customElements.define('open-cells-panel-audio-player', OpenCellsPanelAudioPlayer);

export { OpenCellsPanelAudioPlayer };
