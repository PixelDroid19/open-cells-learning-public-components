import { OpenCellsFormScratch } from './form-scratch.js';

if (customElements.get('open-cells-form-scratch') === undefined) customElements.define('open-cells-form-scratch', OpenCellsFormScratch);

export { OpenCellsFormScratch };
