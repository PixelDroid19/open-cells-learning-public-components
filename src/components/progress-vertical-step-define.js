import { OpenCellsProgressVerticalStep } from './progress-vertical-step.js';

if (customElements.get('open-cells-progress-vertical-step') === undefined) customElements.define('open-cells-progress-vertical-step', OpenCellsProgressVerticalStep);

export { OpenCellsProgressVerticalStep };
