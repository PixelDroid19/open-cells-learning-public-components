import { OpenCellsHeaderCategory } from './header-category.js';

if (customElements.get('open-cells-header-category') === undefined) customElements.define('open-cells-header-category', OpenCellsHeaderCategory);

export { OpenCellsHeaderCategory };
