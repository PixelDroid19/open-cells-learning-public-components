import { OpenCellsExpandableMultistepIndex } from './expandable-multistep-index.js';

if (customElements.get('open-cells-expandable-multistep-index') === undefined) customElements.define('open-cells-expandable-multistep-index', OpenCellsExpandableMultistepIndex);

export { OpenCellsExpandableMultistepIndex };
