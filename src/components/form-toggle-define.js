import { OpenCellsFormToggle } from './form-toggle.js';

if (customElements.get('open-cells-form-toggle') === undefined) customElements.define('open-cells-form-toggle', OpenCellsFormToggle);

export { OpenCellsFormToggle };
