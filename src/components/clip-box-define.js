import { OpenCellsClipBox } from './clip-box.js';

if (customElements.get('open-cells-clip-box') === undefined) customElements.define('open-cells-clip-box', OpenCellsClipBox);

export { OpenCellsClipBox };
