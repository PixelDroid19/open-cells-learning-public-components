import { OpenCellsClipCardData } from './clip-card-data.js';

if (customElements.get('open-cells-clip-card-data') === undefined) customElements.define('open-cells-clip-card-data', OpenCellsClipCardData);

export { OpenCellsClipCardData };
